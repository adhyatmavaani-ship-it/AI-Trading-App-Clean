## Learning Engine Archive

`Learning Engine/` is a local legacy/experimental workspace and is not part of the production app runtime.

Current production integration status:
- Adaptive learning logic was absorbed into the backend service layer.
- Paper simulation flow was aligned with the current trading orchestrator.
- Flutter analytics now exposes adaptive learning state from the backend.

Operational rule:
- Do not treat `Learning Engine/` as a deployable module for this repo.
- Keep it out of Git unless there is a deliberate future migration plan.
